// Transitional Xcode shim. The implementation lives in native/macos so the
// preview decoder is buildable and testable outside the Flutter runner.
#include "../../native/macos/preview_frame_decoder.cpp"
